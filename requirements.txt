# Environment variable management
python-dotenv>=1.0.0,<2.0

# HTTP requests
httpx>=0.27.0,<1.0  # Primary HTTP client (SSL-verified by default)

# Web scraping (for example scripts)
beautifulsoup4>=4.12.3,<5.0

# Data processing
pandas>=2.2.3,<4.0

# Machine Learning
scikit-learn>=1.5.0,<2.0  # Linear regression for trend prediction
numpy>=1.26.0,<3.0  # Numerical computing for ML
scipy>=1.12.0,<2.0  # Statistics for Intelligence Platform forecasting

# Azure DevOps integration
azure-devops>=7.1.0b4,<8.0  # Used in execution/ado_query_bugs.py, execution/create_ado_dec1_baseline.py
msrest>=0.7.1,<1.0  # Authentication for azure-devops SDK (BasicAuthentication)

# ArmorCode integration uses httpx (via execution/http_client.py)

# HTML templating (XSS prevention)
jinja2>=3.1.6,<4.0  # Auto-escaping templates for secure HTML generation

# Testing and security
pytest>=9.0.0,<10.0  # For running security tests
pytest-asyncio>=1.3.0,<2.0  # For async test support

# Observability
sentry-sdk>=2.0.0,<3.0  # Error tracking and performance monitoring
structlog>=24.1.0,<25.0  # Structured logging — used in agents/base_agent.py

# REST API
fastapi>=0.128.7,<1.0  # Modern async web framework (updated to latest: 0.128.7)
uvicorn[standard]>=0.27.0,<1.0  # ASGI server with WebSocket support
python-multipart>=0.0.6,<1.0  # For handling form data

# Interactive charts (Intelligence Platform — Phase A)
plotly>=5.18.0,<6.0  # Interactive chart generation; JS served via CDN (see base_dashboard.html)

# Intelligence Platform — Phase B ML/forecasting stack
duckdb>=0.10.0,<2.0       # Columnar analytics over JSON/Parquet (in-process, zero infra)
ruptures>=1.1.0,<2.0      # Change-point detection
pyarrow>=15.0.0,<24.0     # Parquet I/O for feature store (DuckDB reads natively)

# Testing dependencies
h2>=4.1.0,<5.0  # HTTP/2 support for httpx (required by AsyncSecureHTTPClient)
# Security constraints for transitive dependencies
cryptography>=46.0.5  # CVE-2026-26007 fix
aiohttp>=3.13.3,<4.0  # CVE pin for transitive dep via instructor (not directly imported); >=3.13.3 resolves 9 CVEs
urllib3>=2.6.3  # CVE-2025-50181, CVE-2025-66418, CVE-2025-66471, CVE-2026-21441
black>=26.3.1  # CVE-2026-32274 fix
pyjwt>=2.12.0  # CVE-2026-32597 fix (transitive via msal, botframework-connector)

# MCP (Model Context Protocol) - for skill-based architecture
mcp>=1.0.0,<2.0  # Model Context Protocol SDK for reusable skills

# Production server (Azure deployment)
gunicorn>=22.0.0,<23.0  # WSGI server for production (>=22.0.0 includes security fixes)
google-genai>=1.0.0
