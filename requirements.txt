# Environment variable management
python-dotenv==1.0.0

# HTTP requests
requests>=2.32.3,<3.0  # Security patches for CVE-2023-32681

# Web scraping (for example scripts)
beautifulsoup4>=4.12.3,<5.0
lxml>=5.3.0,<6.0

# Data processing
pandas>=2.2.3,<3.0

# Machine Learning
scikit-learn>=1.5.0,<2.0  # Linear regression for trend prediction
numpy>=1.26.0,<2.0  # Numerical computing for ML

# Azure DevOps integration - using direct REST API (no SDK needed)
# Removed azure-devops==7.1.0b4 beta dependency in favor of direct REST API calls
# See execution/collectors/ado_rest_client.py for REST API implementation

# ArmorCode integration uses requests (no additional SDK needed)

# HTML templating (XSS prevention)
jinja2>=3.1.6,<4.0  # Auto-escaping templates for secure HTML generation

# Testing and security
pytest>=9.0.0,<10.0  # For running security tests
pytest-asyncio>=1.3.0,<2.0  # For async test support

# Observability
sentry-sdk>=1.40.0,<2.0  # Error tracking and performance monitoring

# REST API
fastapi>=0.128.7,<1.0  # Modern async web framework (updated to latest: 0.128.7)
uvicorn[standard]>=0.27.0,<1.0  # ASGI server with WebSocket support
python-multipart>=0.0.6,<1.0  # For handling form data

# Add other dependencies as needed

# Testing dependencies
httpx>=0.27.0,<1.0  # Required by FastAPI TestClient
h2>=4.1.0,<5.0  # HTTP/2 support for httpx (required by AsyncSecureHTTPClient)

# Production server (Azure deployment)
gunicorn>=21.0.0,<22.0  # WSGI server for production
