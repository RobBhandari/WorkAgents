# ========================
# Agentic-Test Configuration Template
# ========================
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.template .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit the .env file to git (it's already in .gitignore)
#
# ========================

# API Keys
# Replace 'your_key_here' with your actual API keys
OPENAI_API_KEY=your_key_here
ANTHROPIC_API_KEY=your_key_here

# Google OAuth Configuration
# After obtaining credentials from Google Cloud Console, update these paths
GOOGLE_CREDENTIALS_PATH=./credentials.json
GOOGLE_TOKEN_PATH=./token.json

# Database Configuration (if needed)
# DATABASE_URL=your_database_url_here

# Email Configuration (for send_email.py)
# For Gmail: Use App Password from https://myaccount.google.com/apppasswords
# For Outlook: Use your account password (ensure SMTP is enabled)
EMAIL_ADDRESS=your_email@example.com
EMAIL_PASSWORD=your_gmail_app_password_here
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Microsoft Graph API Configuration (for send_email_graph.py)
# Get these from Azure Portal: https://portal.azure.com → Azure AD → App registrations
AZURE_TENANT_ID=your_tenant_id_here
AZURE_CLIENT_ID=your_client_id_here
AZURE_CLIENT_SECRET=your_client_secret_here

# Azure DevOps Configuration (for ado_query_bugs.py)
# Get PAT from: https://dev.azure.com/[your-org]/_usersSettings/tokens
ADO_ORGANIZATION_URL=https://dev.azure.com/your-org-name
ADO_PROJECT_NAME=Your Project Name
ADO_PAT=your_personal_access_token_here

# ArmorCode Configuration (for ArmorCode vulnerability tracking)
# Get API key from: ArmorCode UI → Settings → API Keys
ARMORCODE_API_KEY=your_armorcode_api_key_here
ARMORCODE_BASE_URL=https://app.armorcode.com
ARMORCODE_ENVIRONMENT=PRODUCTION
ARMORCODE_PRODUCTS=Product1,Product2,Product3
ARMORCODE_EMAIL_RECIPIENTS=recipient@example.com

# Report Email Recipients (for DOE and multi-project reports)
REPORT_RECIPIENT_EMAIL=recipient@example.com
ARMORCODE_BASELINE_DATE=2025-12-01
ARMORCODE_TARGET_DATE=2026-06-30
ARMORCODE_REDUCTION_GOAL=0.70

# Microsoft Teams Bot Configuration (for teams_bug_bot.py)
# Get these from Azure Bot Service after creation
MICROSOFT_APP_ID=your_microsoft_app_id_here
MICROSOFT_APP_PASSWORD=your_microsoft_app_password_here
PORT=3978

# Observability Configuration (for monitoring and alerting)
# Sentry DSN for error tracking - Get from https://sentry.io
SENTRY_DSN=https://abc123@o123.ingest.sentry.io/456
# Slack webhook for notifications - Get from https://api.slack.com/apps
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00/B00/xxx

# REST API Authentication (for programmatic access to metrics)
# SECURITY: Generate strong, random credentials using the command below
# DO NOT use the placeholder values - they will be rejected by secure_config.py validation
#
# To generate secure credentials, run:
# python -c "import secrets; print(f'API_USERNAME=api_user_{secrets.token_hex(4)}'); print(f'API_PASSWORD={secrets.token_urlsafe(32)}')"
#
# Example output (DO NOT use these exact values - generate your own):
# API_USERNAME=api_user_a1b2c3d4
# API_PASSWORD=xK7mP9nQ2rS5tU8vW3yZ6bC4dE1fG0hJ9kL2mN5oP8qR
#
API_USERNAME=GENERATE_SECURE_USERNAME
API_PASSWORD=GENERATE_SECURE_PASSWORD

# ========================
# SECURITY REMINDER:
# - Keep your .env file secure and never share it
# - Rotate credentials regularly (at least annually)
# - Use strong, unique values for all secrets
# - If credentials are compromised, rotate immediately
# ========================
